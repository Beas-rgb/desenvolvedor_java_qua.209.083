spring.application.name=login

# MySQL Connection - SEM SENHA!
spring.datasource.url=jdbc:mysql://localhost:3306/curso_spring_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=     # ← COMPLETAMENTE VAZIO!

# JPA Settings - Hibernate 7
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Remove a linha do dialect - o Hibernate 7 detecta automaticamente!
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Thymeleaf
spring.thymeleaf.cache=false